add_newline = true

[directory]
truncation_length = 100
truncate_to_repo = false

[aws]
style = "bold blue"
symbol = "🅰 "

[aws.region_aliases]
ap-northeast-1 = "jp"

[custom.arch]
command = "uname -m"
when = """ test $(uname -m) = "x86_64" """
style = "bold yellow"
format = "[$output]($style)"
